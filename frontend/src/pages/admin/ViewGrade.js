import React from 'react';
import { Typography } from '@mui/material';

const ViewGrade = () => (
  <div>
    <Typography variant="h4" gutterBottom>View Grades</Typography>
    <Typography>Grade list will be displayed here.</Typography>
  </div>
);

export default ViewGrade;