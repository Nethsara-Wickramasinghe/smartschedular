import React from 'react';
import { Typography } from '@mui/material';

const ViewTeacher = () => (
  <div>
    <Typography variant="h4" gutterBottom>View Teachers</Typography>
    <Typography>Teacher list will be displayed here.</Typography>
  </div>
);

export default ViewTeacher;