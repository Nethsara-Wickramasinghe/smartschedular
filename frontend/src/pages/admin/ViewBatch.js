import React from 'react';
import { Typography } from '@mui/material';

const ViewBatch = () => (
  <div>
    <Typography variant="h4" gutterBottom>View Batches</Typography>
    <Typography>Batch list will be displayed here.</Typography>
  </div>
);

export default ViewBatch;