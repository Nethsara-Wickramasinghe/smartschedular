import React from 'react';
import { Typography } from '@mui/material';

const ViewVenue = () => (
  <div>
    <Typography variant="h4" gutterBottom>View Venues</Typography>
    <Typography>Venue list will be displayed here.</Typography>
  </div>
);

export default ViewVenue;