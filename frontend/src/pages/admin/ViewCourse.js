import React from 'react';
import { Typography } from '@mui/material';

const ViewCourse = () => (
  <div>
    <Typography variant="h4" gutterBottom>View Courses</Typography>
    <Typography>Course list will be displayed here.</Typography>
  </div>
);

export default ViewCourse;