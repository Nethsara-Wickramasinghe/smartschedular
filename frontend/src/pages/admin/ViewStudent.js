import React from 'react';
import { Typography } from '@mui/material';

const ViewStudent = () => (
  <div>
    <Typography variant="h4" gutterBottom>View Students</Typography>
    <Typography>Student list will be displayed here.</Typography>
  </div>
);

export default ViewStudent;